# 登录存在SQL注入漏洞

- 注入点：登录接口用户名和密码
- payload：1' or 1=1 #

# SQL注入原理

## 1. 原理
SQL注入是由于**应用程序对用户输入的数据未做充分验证和过滤**，直接将不可信内容拼接到SQL语句中，导致数据库执行了攻击者构造的恶意代码。本质上，这是**数据与代码未分离**引发的安全缺陷。

**攻击示例**  
假设后端代码如下：
```php
$username = $_POST['username'];
$password = $_POST['password'];
$sql = "SELECT * FROM users WHERE username = '$username' AND password = '$password'";
```
攻击者输入 `admin' --` 作为用户名，密码任意，则实际执行的SQL变为：
```sql
SELECT * FROM users WHERE username = 'admin' -- ' AND password = 'xxx'
```
注释符 `--` 将后面的密码验证部分注释掉，攻击者成功绕过登录。

## 2. 分类
### 2.1 按注入点位置
| 注入类型 | 说明 |
|----------|------|
| GET注入 | 参数通过URL传递，如 `?id=1` |
| POST注入 | 参数通过表单提交（登录框、搜索框等） |
| Cookie注入 | 注入点位于Cookie值中 |
| HTTP头注入 | 如User-Agent、Referer、X-Forwarded-For等头部字段 |

### 2.2 按数据类型
| 类型 | 特点 |
|------|------|
| 数字型注入 | 参数为整数，无需闭合引号 |
| 字符型注入 | 参数为字符串，需考虑引号闭合 |

### 2.3 按注入技术
| 技术 | 描述 |
|------|------|
| 联合查询注入 | 使用`UNION`将额外查询结果合并到原结果集 |
| 布尔盲注 | 根据页面返回的True/False状态推断数据 |
| 时间盲注 | 利用数据库延时函数（如`sleep()`）判断条件真假 |
| 报错注入 | 利用数据库错误信息直接回显数据 |
| 堆叠查询注入 | 通过分号执行多条SQL语句（部分数据库支持） |

## 3. 常见绕过技巧
攻击者常用以下手法绕过简单的输入过滤：

- **注释符**：`/**/`、`--`、`#` 等注释后续代码。
- **大小写混合**：如 `SeLeCt` 绕过大小写过滤。
- **内联注释**：MySQL特有的 `/*!...*/`，内部SQL会被执行。
- **等价替换**：`AND` 替换 `&&`，`CONCAT()` 替换 `+`。
- **编码绕过**：URL编码、双重编码、Hex编码等。
- **空格替代**：`/**/`、制表符、换行符代替空格。
- **特殊截断**：`%00`、`%0a` 等截断字符串。

## 4. 修复方案
### 4.1 根本性防御
- **预编译语句（参数化查询）**  
  使用 `PreparedStatement`（Java）、PDO（PHP）等机制，将SQL语句结构与用户输入分离。
  ```java
  // Java示例
  String sql = "SELECT * FROM users WHERE username = ? AND password = ?";
  PreparedStatement ps = connection.prepareStatement(sql);
  ps.setString(1, username);
  ps.setString(2, password);
  ResultSet rs = ps.executeQuery();
  ```

- **输入验证**  
  对用户输入进行严格的白名单校验：数字类型强制转换，字符串限定长度、格式（如邮箱正则）。

- **最小权限原则**  
  数据库账户仅授予必要权限（如只读账户不可写，禁止执行文件操作或调用系统函数）。

### 4.2 辅助防护
- **隐藏错误信息**：关闭详细数据库错误输出，使用自定义错误页面。
- **部署WAF**：Web应用防火墙拦截恶意SQL注入载荷。
- **使用ORM框架**：如Hibernate、MyBatis，但需注意避免 `${}` 拼接。
- **定期安全审计**：代码审计、自动化扫描工具检测注入点。

## 5. 总结
SQL注入是Web安全中最常见且危害极大的漏洞之一。开发者必须始终以“**用户输入不可信**”为原则，通过参数化查询、输入验证、最小权限等手段构建纵深防御，从源头杜绝数据与代码的混合，确保应用安全。
